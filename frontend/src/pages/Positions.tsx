import Layout from '../components/Layout'
import PositionsComponent from '../components/Positions'

/**
 * Página de Posiciones
 * Muestra el histórico de posiciones de las carteras
 */
export default function Positions() {
    return (
        <Layout>
            <PositionsComponent />
        </Layout>
    )
}
